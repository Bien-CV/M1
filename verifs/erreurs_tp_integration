erreurs trouvée :

dans AirwayImpl:
grâce a Junit : on voit que les parametres sont inversés dans le constructeur. start et stop

Dans AirplaneImpl:
on voit que les parametres sont inversés dans le constructeur. c et t
